{
  "name": "msgpackr-extract",
  "author": "Kris Zyp",
  "version": "1.0.16",
  "description": "Node addon for string extraction for msgpackr",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "http://github.com/kriszyp/msgpackr-extract"
  },
  "scripts": {
    "install": "node-gyp-build",
    "recompile": "node-gyp build",
    "before-publish": "prebuildify-ci download && prebuildify --napi",
    "prebuild": "prebuildify --target 17.2.0 && prebuildify --napi",
    "prebuild-musl": "prebuildify --napi --libc musl --tag-libc",
    "prebuild-arm64": "prebuildify --napi --arch=arm64 --tag-libc",
    "test": ""
  },
  "main": "./index.js",
  "gypfile": true,
  "dependencies": {
    "node-addon-api": "^4.3.0",
    "node-gyp-build": "^4.2.3"
  },
  "devDependencies": {
    "prebuildify": "kriszyp/prebuildify#b78c5a9",
    "prebuildify-ci": "^1.0.5"
  }
}
